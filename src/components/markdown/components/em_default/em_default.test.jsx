import { renderTheme } from '../../../styles/render-theme';
import { EmDefault } from './em_default';
describe('<EmDefault />', () => {
  it('should render', () => {
    renderTheme(<EmDefault />)
  })
})
