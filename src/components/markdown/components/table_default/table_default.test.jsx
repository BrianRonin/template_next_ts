import { renderTheme } from '../../../styles/render-theme';
import { TableDefault } from './table_default';
describe('<TableDefault />', () => {
  it('should render', () => {
    renderTheme(<TableDefault />)
  })
})
