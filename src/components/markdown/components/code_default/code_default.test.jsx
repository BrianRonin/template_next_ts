import { renderTheme } from '../../../styles/render-theme';
import { CodeDefault } from './code_default';
describe('<CodeDefault />', () => {
  it('should render', () => {
    renderTheme(<CodeDefault />)
  })
})
