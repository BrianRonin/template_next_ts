import { renderTheme } from '../../../styles/render-theme';
import { ImgDefault } from './img_default';
describe('<ImgDefault />', () => {
  it('should render', () => {
    renderTheme(<ImgDefault />)
  })
})
