import { {{ properCase name }} as {{ camelCase element }} } from './{{ snakeCase name }}'
import { MarkdownRender } from '../../render/markdown_render'

export default {
  title: 'markdown/{{ group }}',
  component: {{ camelCase element }},
  args: {
    components: {
      {{ camelCase element }}
    }
  },
  argTypes: {
    //
  },
}

export const Template = (args) => <MarkdownRender {...args} />

Template.parameters = {
  backgrounds: {
    default: 'light'
  },
}
