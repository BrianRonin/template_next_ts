import * as S from './S.{{ snakeCase name }}'

export const {{ properCase name }} = ({ node, className, children, ...props }) => {
  return (
    <S.Main>
      <{{ camelCase element }} className {...props} />
    </S.Main>
  )
}
