import { renderTheme } from '../../styles/render-theme';
import { {{ properCase name }} } from './{{ snakeCase name }}';
describe('<{{ properCase name }} />', () => {
  it('should render', () => {
    renderTheme(<{{ properCase name }} />)
  })
})
