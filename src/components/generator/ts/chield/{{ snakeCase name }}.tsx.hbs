import * as S from './S.{{ snakeCase name }}'

export type {{ camelCase name }}Props = {
  children?: React.ReactNode
}

export const {{ properCase name }} = ({ children }: {{ camelCase name }}Props) => {
  return <S.Main>{children}</S.Main>
}
